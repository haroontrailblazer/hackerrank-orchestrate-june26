# Argus - Multi-Modal Evidence Review
# Python 3.10+ recommended.
pandas>=2.0
pydantic>=2.0
pillow>=10.0
python-dotenv>=1.0
tenacity>=8.0
# Provider SDKs are optional: install the one you intend to run with.
# The offline "mock" provider needs neither.
anthropic>=0.40 ; extra == "anthropic"
openai>=1.40 ; extra == "openai"

# Dev / tests
pytest>=8.0 ; extra == "dev"
